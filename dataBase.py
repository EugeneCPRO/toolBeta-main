


# file i/o class

class dataBase(object):
        def __init__(self, init):
            self.init = init

# check if a file already exists
def checkExist(file): # files are assigned on a per name basis (see main)

    return

# scrub file from database 
def deleteFile(file):
     
    return

# format data for indexing, assign name 
def formatData(data):

    return

# read from data base
def readFrom(file):
      
    return

# write data to file
def writeTo(file, data): # this should dynamically alter, append, remove data
    
    return


